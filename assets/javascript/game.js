
//Sets the user aggregate variable to 0 to start game
var userAggregate = 0;

//Lets the computer select a random letter from 19 to 120
var compRandom = Math.floor(Math.random() * 102 + 19);

//Create and set variable for each of the crystals when clicked.
var crystalRandom = Math.floor(Math.random() * 12 + 1);


// Function will be called when we reset everything
var reset = function() {
                        userAggregate = 0;
    
                        }


